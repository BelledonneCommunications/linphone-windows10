<phone:PhoneApplicationPage
    x:Class="Linphone.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- LOCALIZATION NOTE:
            To localize the displayed strings copy their values to appropriately named
            keys in the app's neutral language resource file (AppResources.resx) then
            replace the hard-coded text value between the attributes' quotation marks
            with the binding clause whose path points to that string name.

            For example:

                Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}"

            This binding points to the template's string resource named "ApplicationTitle".

            Adding supported languages in the Project Properties tab will create a
            new resx file per language that can carry the translated values of your
            UI strings. The binding in these examples will cause the value of the
            attributes to be drawn from the .resx file that matches the
            CurrentUICulture of the app at run time.
         -->

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            
            <toolkit:PhoneTextBox 
                x:Name="sipAddress"
                AcceptsReturn="False"
                Grid.Row="0"
                Hint="{Binding Path=LocalizedResources.AddressHint, Source={StaticResource LocalizedStrings}}"
                ActionIcon="/Assets/backspace_default.png"
                ActionIconTapped="sipAddress_ActionIconTapped_1">
            </toolkit:PhoneTextBox>
            
            <Grid Grid.Row="1" Margin="-20, 10, -20, 10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" x:Name="one" Click="digit_Click_1" Tag="1">
                    <Button.Template>
                        <ControlTemplate>
                            <Image Stretch="UniformToFill" Source="/Assets/Numpad/numpad_one_default.png"/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Grid.Column="1" x:Name="two" Click="digit_Click_1" Tag="2">
                    <Button.Template>
                        <ControlTemplate>
                            <Image Stretch="UniformToFill" Source="/Assets/Numpad/numpad_two_default.png"/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Grid.Column="2" x:Name="three" Click="digit_Click_1" Tag="3">
                    <Button.Template>
                        <ControlTemplate>
                            <Image Stretch="UniformToFill" Source="/Assets/Numpad/numpad_three_default.png"/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <Grid Grid.Row="2" Margin="-20, 10, -20, 10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" x:Name="four" Click="digit_Click_1" Tag="4">
                    <Button.Template>
                        <ControlTemplate>
                            <Image Stretch="UniformToFill" Source="/Assets/Numpad/numpad_four_default.png"/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Grid.Column="1" x:Name="five" Click="digit_Click_1" Tag="5">
                    <Button.Template>
                        <ControlTemplate>
                            <Image Stretch="UniformToFill" Source="/Assets/Numpad/numpad_five_default.png"/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Grid.Column="2" x:Name="six" Click="digit_Click_1" Tag="6">
                    <Button.Template>
                        <ControlTemplate>
                            <Image Stretch="UniformToFill" Source="/Assets/Numpad/numpad_six_default.png"/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <Grid Grid.Row="3" Margin="-20, 10, -20, 10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" x:Name="seven" Click="digit_Click_1" Tag="7">
                    <Button.Template>
                        <ControlTemplate>
                            <Image Stretch="UniformToFill" Source="/Assets/Numpad/numpad_seven_default.png"/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Grid.Column="1" x:Name="eight" Click="digit_Click_1" Tag="8">
                    <Button.Template>
                        <ControlTemplate>
                            <Image Stretch="UniformToFill" Source="/Assets/Numpad/numpad_eight_default.png"/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Grid.Column="2" x:Name="nine" Click="digit_Click_1" Tag="9">
                    <Button.Template>
                        <ControlTemplate>
                            <Image Stretch="UniformToFill" Source="/Assets/Numpad/numpad_nine_default.png"/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <Grid Grid.Row="4" Margin="-20, 10, -20, 10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" x:Name="star" Click="digit_Click_1" Tag="*">
                    <Button.Template>
                        <ControlTemplate>
                            <Image Stretch="UniformToFill" Source="/Assets/Numpad/numpad_star_default.png"/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Grid.Column="1" x:Name="zero" Click="digit_Click_1" Tag="0">
                    <Button.Template>
                        <ControlTemplate>
                            <Image Stretch="UniformToFill" Source="/Assets/Numpad/numpad_zero_default.png"/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Grid.Column="2" x:Name="sharp" Click="digit_Click_1" Tag="#">
                    <Button.Template>
                        <ControlTemplate>
                            <Image Stretch="UniformToFill" Source="/Assets/Numpad/numpad_sharp_default.png"/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

        </Grid>

        <!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
        <!--<Image Source="/Assets/AlignmentGrid.png" VerticalAlignment="Top" Height="800" Width="480" Margin="0,-32,0,0" Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False" />-->
    </Grid>

</phone:PhoneApplicationPage>