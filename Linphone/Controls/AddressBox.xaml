<UserControl x:Class="Linphone.Controls.AddressBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:controls="clr-namespace:Linphone.Controls"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="100" d:DesignWidth="480">
    
    <UserControl.Resources>
        <ImageBrush ImageSource="/Assets/dialer_address_background.png" x:Key="FieldBackground"/>
        <Style TargetType="ContentControl" x:Key="HintWhiteStyle">
            <Setter Property="Foreground" Value="White"></Setter>
        </Style>
        <Style TargetType="toolkit:PhoneTextBox" x:Key="TransparentFocus">
            <Setter Property="Background" Value="Transparent"></Setter>
            <Setter Property="Foreground" Value="White"></Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid x:Name="LayoutRoot" VerticalAlignment="Center" Background="{StaticResource FieldBackground}">
        <toolkit:PhoneTextBox 
            x:Name="address"
            Text="{Binding Text}"
            FontSize="40"
            AcceptsReturn="False" 
            BorderThickness="0"
            Style="{StaticResource TransparentFocus}"
            HintStyle="{StaticResource HintWhiteStyle}"
            Hint="{Binding Path=LocalizedResources.AddressHint, Source={StaticResource LocalizedStrings}}"/>

        <Image  
            x:Name="backspace"
            Source="/Assets/backspace_default.png" 
            Stretch="Uniform"
            Width="75"
            Hold="backspace_Hold_1"
            Tap="backspace_Tap_1"
            ManipulationStarted="Image_ManipulationStarted_1"
            ManipulationCompleted="Image_ManipulationCompleted_1"
            VerticalAlignment="Center"
            HorizontalAlignment="Right"
            Margin="0,0,20,0"/>
    </Grid>
</UserControl>